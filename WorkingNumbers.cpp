#include <iostream>
#include <cmath>
using namespace std;
int main()
{
    int num1 = 3;
    double num2 = 35.50;
    num1++;     /*icrements */
    num1 += 30; /*adds 30 to the number similar can be used for multiply divide etc */
    cout << pow(2, 3) << fmax(3, 9) << endl;

    return 0;
}